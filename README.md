# v8std-standards-harness

**Fail-closed обвязка для LLM-агентов вокруг стандартов разработки 1С:Предприятие.**

Скилл + карта триггеров + машинный гейт, которые не дают агенту молча пропустить сверку
со стандартами (`v8std`) при работе с BSL и метаданными.

![Схема: работа агента проходит через четыре ступени контроля](assets/four-layers.png)

<p align="center">
  <sub>Работа агента идёт слева направо через четыре ступени. <b>TRIGGER MAP</b> — карта в двух проекциях
  (по объекту метаданных и по действию в коде) выдаёт номера стандартов. <b>SEARCH</b> и <b>SENTINEL</b> обращаются
  к внешнему сервису <b>v8std MCP</b>: первый ищет то, чего нет в карте, второй проверяет, что сервис вообще жив.
  <b>VALIDATOR</b> — затвор: пропускает в коммит только при наличии <b>EVIDENCE</b>, иначе <b>BLOCK</b>.</sub>
</p>

---

## Проблема

Вы допустили LLM-агента к боевому BSL и написали в инструкции: «сверяйся со стандартами
разработки».

Агент отвечает «сверился». Иногда действительно сверяется. **Отличить одно от другого
невозможно** — невыполнение инструкции выглядит ровно так же, как выполнение. Узнаёте вы
об этом на ревью, а чаще — в проде.

Дело не в недобросовестности модели. Стандартов около трёхсот; без карты «в этой ситуации
смотри вот эти» проверка превращается в «вспомни всё, что знаешь про 1С». Модель вспоминает
приблизительно — и уверенно.

## Решение: четыре слоя

Ни один слой не самодостаточен. Каждый закрывает дыру, оставленную остальными.

| Слой | Что ловит | Что протекает без него |
|---|---|---|
| **1. Карта триггеров** | Известные ситуации: «пишешь запрос → std729/437/438» | Агент сверяется по настроению и по памяти |
| **2. Обязательный поиск** | Стандарты вне карты — минимум один `v8std_search` за фазу | Карта замерзает в моменте создания |
| **3. Sentinel** | Протухший или недоступный индекс | «Ничего не нашлось» неотличимо от «сервис лежит» |
| **4. Валидатор** | Саму молчаливую деградацию — отсутствие следа проверки | Слои 1–3 остаются рекомендацией |

Ключевая идея четвёртого слоя: **пропуск разрешён, молчаливый пропуск — нет.** Каждая
проверка оставляет одну машинно-читаемую строку, и `skipped` с причиной из закрытого списка
— полноправный, ожидаемый исход. Невозможен только третий вариант: не проверить и не
сказать об этом.

```
[v8std applied:   phase=implement, scope=document-posting, ids_checked=[std450,std603], conclusion=clean]
[v8std skipped:   phase=implement, scope=query-virtual-table, planned_ids=[std657], reason=timeout, retries=3]
[v8std discovered: phase=implement, scope=document-posting, query="проведение движения", top_ids=[std450,std792], new_ids=[std792], decision=applied]
[v8std sentinel:  id=std450, status=found, phase=implement]
```

## Что внутри

| Файл | Назначение |
|---|---|
| [`SKILL.md`](SKILL.md) | Скилл: когда срабатывает, что делать, формат записей |
| [`references/standards-map.md`](references/standards-map.md) | Карта по **объекту метаданных**: «создаёшь регистр → …», «настраиваешь RLS → …» |
| [`references/situations.md`](references/situations.md) | Карта по **действию в коде**: «пишешь `ОбработкаПроведения` → …», «составной тип в запросе → …» |
| [`references/INSTALL.md`](references/INSTALL.md) | Пошаговая установка — рассчитана на выполнение агентом |
| [`references/field-notes.md`](references/field-notes.md) | Почему слоёв именно четыре: инциденты, из которых каждый вырос |
| [`tools/`](tools/) | Валидатор, смоук-тест (19 кейсов), пример конфига |

## Быстрый старт

```bash
# 1. Подключить MCP стандартов (Claude Code — .mcp.json, Codex — ~/.codex/config.toml)
#    { "mcpServers": { "v8std": { "type": "http", "url": "https://ai.v8std.ru/mcp" } } }

# 2. Проверить, что сервис отвечает
#    v8std_get_page("std450")  →  found=true

# 3. Проверить валидатор
node tools/v8std-evidence-validator.smoke.mjs      # 19 passed, 0 failed

# 4. Прогнать гейт
node tools/v8std-evidence-validator.mjs validate-pack . --gate
```

Подробно, включая подключение к CI и сбор офлайн-кэша, — [`references/INSTALL.md`](references/INSTALL.md).

## Два профиля валидатора

| Профиль | Поведение | Для чего |
|---|---|---|
| `lint` | Проверяет формат найденного. Ноль записей = чисто | Ручной прогон |
| `gate` | Fail-closed: минимум одна запись, sentinel с совпадающим `id`, discovery или обоснованный skip. **Ноль записей = ошибка** | CI, pre-commit |

Разделение появилось после того, как выяснилось: валидатор на каталоге без единой записи
возвращал «чисто». То есть проект, где стандарты не применялись вообще — ровно целевой
отказ — проходил гейт зелёным. **У валидатора «ничего не найдено» и «всё в порядке»
обязаны быть разными исходами.**

## Требования

- 1С:Предприятие (BSL) — карты предметно привязаны к платформе
- LLM-агент, пишущий или ревьюящий код
- Node.js 18+ для валидатора (внешних зависимостей нет)
- Доступ к `https://ai.v8std.ru/mcp` — либо офлайн-кэш, собранный по инструкции

## Не на 1С?

Содержимое карт вам не подойдёт, но переносимо главное — паттерн проверяемого пропуска:
типы записей, обязательные поля, различие `lint`/`gate` и правило «ноль записей ≠ чисто».
Раздел «Перенос идеи на другой стек» в [`references/INSTALL.md`](references/INSTALL.md).

## Границы честности

Пакет не притворяется тем, чем не является:

- **Не анализирует код.** `v8std` — справочник стандартов и диагностик, он не заменяет
  синтаксическую проверку, статический анализатор и тесты.
- **Не гарантирует правильность применения.** Валидатор проверяет наличие и формат следа,
  а не качество решения. Это защита от пропуска, а не от ошибки.
- **Sentinel не доказывает актуальность карты.** Он ловит недоступность сервиса и откат
  индекса назад, но не то, что индекс ушёл вперёд, а карта осталась в прошлом.
- **`smell_patterns` сегодня исполняемого потребителя не имеют** — это структурированная
  подсказка для агента, а не автоматический детектор.
- **Карта — снимок на дату** (`last_reviewed_at`). Ревизия — ваша ответственность.

## Атрибуция

Тексты стандартов разработки 1С принадлежат правообладателям: первоисточник — ИТС фирмы
«1С» (`its.1c.ru/db/v8std`), удобный доступ — [v8std.ru](https://v8std.ru/) и его MCP-сервис.

**Этот репозиторий текстов стандартов не содержит.** В картах — только идентификаторы,
официальные заголовки как средство идентификации и собственные формулировки авторов о том,
в какой рабочей ситуации к стандарту следует обратиться. Содержание требований читается по
ссылке у правообладателя. См. [`LICENSE`](LICENSE).

Отдельная благодарность авторам [v8std.ru](https://v8std.ru/) — без их MCP-сервиса
машинная работа со стандартами была бы куда менее удобной.

## Лицензия

MIT — на код, тесты, документацию и карты соответствия этого репозитория.
Подробнее об области действия — [`LICENSE`](LICENSE).
